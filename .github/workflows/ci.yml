name: CI

on:
  push:

jobs:
  python-build:
    name: "python build"
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Poetry
        run: |
          python -m pip install --upgrade pip
          pip install poetry

      - name: Install dependencies
        run: poetry install --with dev

      - name: Lint (ruff)
        run: poetry run ruff check .

      - name: Format check (black)
        run: poetry run black --check .

  # docker-build:
  #   name: "Docker build"
  #   runs-on: ubuntu-latest
  #   needs: python-build
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     # Schafft ca. 30GB Platz auf dem GitHub Runner (Löst das Speicherproblem)
  #     - name: Free Disk Space (Ubuntu)
  #       uses: jlumbroso/free-disk-space@v1.3.1
  #       with:
  #         tool-cache: true
  #         android: true
  #         dotnet: true
  #         haskell: true
  #         large-packages: true
  #         docker-images: true

  #     # Richtet Buildx ein, um modernes Caching zu ermöglichen
  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     # Baut das Image und nutzt den GitHub Actions Cache
  #     - name: Build Docker image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: .
  #         push: false
  #         tags: handpose:ci
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max
